/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images : {
      domains : ['content.schwab.com'] // <== Domain name
    }
};

export default nextConfig;

